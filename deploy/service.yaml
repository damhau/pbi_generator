apiVersion: v1
kind: Service
metadata:
  name: pbi-generator
  labels:
    app: pbi-generator
spec:
  selector:
    app: pbi-generator
  ports:
    - port: 8080
      targetPort: 5000
      protocol: TCP
